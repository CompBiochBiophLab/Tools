# Statistical tools

This folder contains a collection of tools to plot distrubutions

## Environment

Use the folder environment file before running the notebooks:

```bash
conda env create -f statsTools/environment.yml
conda activate tools-statstools
```

When the environment is no longer needed, remove it with:

```bash
conda deactivate
conda env remove -n tools-statstools
```
