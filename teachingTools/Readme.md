# Teaching tools

Small scripts to handle teaching duties

## Environment

Use the folder environment file before running the notebooks:

```bash
conda env create -f teachingTools/environment.yml
conda activate tools-teachingtools
```

When the environment is no longer needed, remove it with:

```bash
conda deactivate
conda env remove -n tools-teachingtools
```
