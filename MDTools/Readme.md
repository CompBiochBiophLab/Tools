# Molecular dynamics tools

diverse tools for molecular dynamics simulations and analysis.

## Environment

Use the folder environment file before running the notebooks:

```bash
conda env create -f MDTools/environment.yml
conda activate tools-mdtools
```

When the environment is no longer needed, remove it with:

```bash
conda deactivate
conda env remove -n tools-mdtools
```
