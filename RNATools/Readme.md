# RNA Tools

Some tools for transcriptomics analysis

## Environment

Use the folder environment file before running the notebooks:

```bash
conda env create -f RNATools/environment.yml
conda activate tools-rnatools
```

When the environment is no longer needed, remove it with:

```bash
conda deactivate
conda env remove -n tools-rnatools
```
