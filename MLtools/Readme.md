# ML tools

Small scripts to teach ML procedures.

## Environment

Use the folder environment file before running the notebooks:

```bash
conda env create -f MLtools/environment.yml
conda activate tools-mltools
```

When the environment is no longer needed, remove it with:

```bash
conda deactivate
conda env remove -n tools-mltools
```
