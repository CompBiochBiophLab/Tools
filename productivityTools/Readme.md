# Productivity tools

How to create a gantt chart and other tools

## Environment

Use the folder environment file before running the notebooks:

```bash
conda env create -f productivityTools/environment.yml
conda activate tools-productivity
```

When the environment is no longer needed, remove it with:

```bash
conda deactivate
conda env remove -n tools-productivity
```
