# Python primer

This folder contains some very simple notebooks for a fast introduction to python programming

## Environment

Use the folder environment file before running the notebooks:

```bash
conda env create -f pythonPrimer/environment.yml
conda activate tools-pythonprimer
```

When the environment is no longer needed, remove it with:

```bash
conda deactivate
conda env remove -n tools-pythonprimer
```
