# PDB tools

Tools to manipulate protein structures

## Environment

Use the folder environment file before running the notebooks:

```bash
conda env create -f PDBTools/environment.yml
conda activate tools-pdbtools
```

When the environment is no longer needed, remove it with:

```bash
conda deactivate
conda env remove -n tools-pdbtools
```
